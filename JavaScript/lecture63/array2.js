// join method of the array

let arr = [23,45,56,78,90]

console.log(arr.join("and")) // elements of the array will be separated by the and 

console.log(arr.join(" "))// elements of the array will  be separated by the space

// pop method of the arrAY removes the last element from the array and return the removed element

console.log(arr.pop())

console.log(arr)

// push method of the array appends the element to the end of the array and return the updated length of the array

console.log(arr.push("Sahil"))

console.log(arr)

// shift method  removes the elements from the start of the array whrer as pop removes element from the end of the array

console.log(arr.shift())

console.log(arr)

// unshift method adds element ti the start of the array where as push method add element to the end of the array

console.log(arr.unshift('unshift'))

console.log(arr)